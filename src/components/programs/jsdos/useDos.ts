import { useRef } from "react";
import { type CommandInterface } from "emulators";
import { type DosInstance } from "emulators-ui/dist/types/js-dos";

export function useDos() {
  const a: DosInstance = null;
}
